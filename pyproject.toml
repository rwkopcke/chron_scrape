[project]
name = "chron-scrape"
version = "1.0.0"
description = "Fetch data from SP and FRED for earn-price project"
readme = "README.md"
requires-python = ">=3.12.4"
dependencies = [
    "requests>=2.32.5",
]
